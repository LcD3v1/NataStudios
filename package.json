{
  "name": "nata-studios",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "NATA STUDIOS — site institucional e plataforma interna (Express + Vite).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --include=dev && npm --prefix backend install --include=dev && npm --prefix frontend install --include=dev",
    "build": "npm run install:all && npm --prefix frontend run build && npm --prefix backend run build",
    "start": "node index.js",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "test:security": "npm --prefix backend run test:security"
  },
  "devDependencies": {}
}
